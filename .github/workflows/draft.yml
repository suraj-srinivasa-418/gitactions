name: basic-deploy

on: 
  push:
    branches: 
      - main
      
jobs:
  deploy:
    runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
    - run: npm install
     env:
      DEFAULT_BRANCH: main
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


